import {
  combine,
  comments,
  ignores,
  imports,
  javascript,
  jsdoc,
  jsonc,
  markdown,
  node,
  stylistic,
  toml,
  typescript,
  unicorn,
  vue,
  yaml,
} from '@antfu/eslint-config'

export default combine(
  ignores(),
  javascript(),
  typescript(),
  imports(),
  vue(),
  node(),
  jsdoc(),
  jsonc(),
  yaml(),
  toml(),
  markdown(),
  stylistic(),
  comments(),
  unicorn(),

)
